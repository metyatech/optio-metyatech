# Agent Control Plane - Specification

## Goal

Convert Optio into a personal AI-agent task control plane.

## Key Features

- Single and Multi-repo task support.
- Persistent repository cache (replacing git worktree).
- OpenCode custom runtime adapter integrated with agent-dotfiles.
- Agent Profile Manager for auth state.
- Per-repository merge queue instead of GitHub Merge Queue.
